<div class='nested-fields'>
    <%# <%= f.association :procedure, collection: Procedure.joins(:service).order %>
    <%= f.select :procedure_id, Procedure.all.map { |r| ["#{r.service.title} | #{r.dentist.user.first_name}", r.id ] }, include_blank: true %>
    <%= link_to_remove_association "Remover Procedimento", f %>
</div>